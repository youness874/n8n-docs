services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kasbahai
      POSTGRES_PASSWORD: kasbahai
      POSTGRES_DB: kasbahai
    ports:
      - '5432:5432'
    volumes:
      - kasbahai_postgres_data:/var/lib/postgresql/data

volumes:
  kasbahai_postgres_data:
